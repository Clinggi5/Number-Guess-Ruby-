puts 'Welcome to the number guessing game.  I will pick a number between 1-100.  It will be your job to guess the number.  If you are incorrect, I will tell you if your guess is higher or lower than my number.'
puts "Guess a number!"
mine = (rand(1..100))
while TRUE
	grabber = gets.chomp!
	if mine == grabber.to_i
	puts 'That\'s it!'
	break
	elsif mine > grabber.to_i
	puts 'My number is larger than yours'
	elsif mine < grabber.to_i 
	puts 'My number is less than yours'
	end
end
